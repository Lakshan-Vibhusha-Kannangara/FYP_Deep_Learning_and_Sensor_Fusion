<?xml version="1.0"?>
<launch>

  <!-- Load joint controller configurations from YAML file to parameter server -->
  <rosparam file="$(find jaguar)/config/ros_controllers.yaml" command="load"/>

  <!-- Load the controllers -->

</launch>
